'use client';

/**
 * @deprecated Remplacé par PricingStudioNav (sous-nav Studio Prix).
 * Conservé pour imports legacy — redirige vers la nouvelle nav via le hub.
 */
export { PricingStudioNav as PricingFamilyCards } from './PricingStudioNav';
