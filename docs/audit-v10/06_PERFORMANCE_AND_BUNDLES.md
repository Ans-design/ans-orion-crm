# Performance et bundles (V10)

- Build : `NODE_OPTIONS=--max-old-space-size=4096` documenté (filet, pas seule optimisation).
- Pages Admin lourdes : lazy-load progressif.
- Dashboard : remplacer findMany massifs par agrégats.

Mesures avant/après : à renseigner après PERF lots.
