# Parité prix (V10)

Règle : base **publiée** = unique vérité runtime.  
Excel / constantes / localStorage = brouillon / import uniquement.

Si DB indisponible ou tarif manquant ⇒ `PRICE_UNAVAILABLE` (pas de fallback silencieux).

Fixtures : `tests/` + snapshots `docs/audit-v10/pricing-fixtures/` (à enrichir).
