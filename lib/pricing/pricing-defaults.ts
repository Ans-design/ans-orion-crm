/**
 * Constantes pricing sûres pour le client (POS, etc.).
 * Ne pas importer prisma / fs ici — voir `global-config.ts` pour le chargement DB.
 */
export const DEFAULT_FACE_RECTO_VERSO_MULT = 1.8;
export const DEFAULT_FINITION_SURCHARGE_PCT = 12;
