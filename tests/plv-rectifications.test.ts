/**
 * Placeholder — suite PLV rectifications (fichier ne doit pas rester vide pour vitest).
 * Les règles PLV live sont couvertes par tests/plv-*.test.ts et lib/data/plv-tariffs.ts.
 */
import { describe, expect, it } from 'vitest';

describe('plv-rectifications', () => {
  it('suite réservée — pas de fichier de test vide', () => {
    expect(true).toBe(true);
  });
});
